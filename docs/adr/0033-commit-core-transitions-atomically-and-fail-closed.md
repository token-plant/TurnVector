# Commit Core Transitions Atomically and Fail Closed

Atomic Core Transition and Hot-Path Work Budget rules apply to every Serving Profile. Compatibility Custody and Bounded Integrity Lifecycle mechanisms in this decision apply only to the Integrity Serving Profile and cannot gate Service Readiness under the P0 Serving Profile.

Compatibility Custody Bounded Integrity Lifecycle Transition Publication Forward Reconciliation Evidence binds the original transition, native publication identity and Receipt, and authoritative read-only readback of that exact publication. Forward Reconciliation accepts an outcome only when two Jobs satisfying Independent Job Failure-Domain Proof byte-agree; current state, cache, logs, time, one Job or evidence from another transition selects neither predecessor nor successor.

Forward Reconciliation uses only the pinned Lifecycle's finite attempts, deadlines and terminal capacity. Timeout or incomplete evidence selects no state; exhaustion atomically enters the Self-Integrity Terminal, preserves the complete affected fence and permits no extra attempt, capacity borrowing, reset or elapsed-time inference.

Opening a Lifecycle permanently pins its exact Schema Vector, Catalog generation, Transition Table, Capacity Formula and required reader set; successors govern only new Lifecycle identities. If a transition publication may have committed but cannot be proved, the Lifecycle becomes nonready and neither predecessor nor proposal is selected. Its sole exit is read-only Forward Reconciliation proving the exact original transition or committing the predeclared Self-Integrity Terminal, without rollback, replay, republish or recursive recovery.

Each Catalog generation binds one complete total Lifecycle Transition Table. Self-Integrity Terminal, Authority Loss and Capacity Exhaustion have no outgoing authority transition, Late Evidence remains nonauthoritative, and every valid state/outcome pair has exactly one successor or rejection independent of event arrival order; specializations and current software cannot add edges.

Every specialization uses the closed Lifecycle Outcome Algebra and may carry only its typed domain result; it cannot add a new authority class or collapse a nonauthoritative terminal into success. Failure to derive, read or publish the Lifecycle itself atomically commits its predeclared Self-Integrity Terminal, fences the complete affected universe and creates no recursive Lifecycle; later continuation crosses only the identity-bound logical or physical root boundary.

Simultaneous integrity failures form one immutable Integrity Incident Set without priority replacement, root-cause collapse or latest-wins behavior. Each Incident retains its separately derived Closure; the effective Composite Authority Fence unions all affected universes and intersects permitted actions so the most restrictive applicable action governs without changing any underlying Incident or evidence identity.

Late Index, Source or Profile omission atomically invalidates the complete Closure and partitions every directly or transitively affected external Effect: proven-unexposed work cancels, while started or ambiguous work may perform only exact read-only reconciliation to a nonauthoritative terminal. Reconciliation-proof disagreement is per Operation and bounded; no Effect-DAG vote, mutation repair, late proof, restored query or fresh certification reopens an old terminal or releases its complete fence.

Late cyclic-member readback, proof-query restoration or certification never reopens an Indeterminate terminal, clears its dependent fences or grants retrospective authority. A fresh Profile omission or proof revocation partitions the complete affected Effect closure atomically: unexposed Effects cancel, while started or ambiguous Effects may perform only exact read-only reconciliation to a nonauthoritative terminal. External semantic mutation likewise fences its complete operation, proposal, reconciliation and Tool/Capability closure in one fail-closed transition.

Each cyclic Effect member owns an independent bounded read-only disagreement chain; exhaustion is member-level Indeterminate and no cycle-wide vote exists. Late evidence proving an out-of-contract mutation creates a separate semantic-integrity incident without rewriting proposal activation, and a revoked-proof Effect lacking a certified read-only query terminates Indeterminate without issuing a broader or incident-time query.

Cyclic Effects that already started are treated as started but receive only independent read-only reconciliation in canonical identity order; that audit order grants no dependency authority. Revoked-proof Effects use the same exposed-versus-unexposed partition, late proof cannot reopen exposure exhaustion, and process exit without verified ownership release remains a nonready repair boundary rather than a takeover opportunity.

An affected-Effect dependency cycle is a root-integrity incident, not an SCC scheduling opportunity. Conflicting exposure proofs advance only through a bounded two-Job chain whose exhaustion is Indeterminate with the complete scope treated as started; a Critical termination that cannot prove process and ownership release keeps the service nonready, retains all reclaim charges and forbids a replacement owner.

Omission-affected Effects are handled as one canonical dependency DAG: started or ambiguous ancestors reconcile in topological order, unexposed descendants cancel, and no descendant starts from a nonauthoritative result. Exposure may resolve only before its Indeterminate Terminal; later evidence is audit-only, and proposal activation consumes a terminal-drain fact rather than inferred success.

Uncertain external Effect delivery is treated as possibly started: no mutation is retransmitted, and only the exact certified read-only path may reconcile the complete possible scope. Omission Incidents apply the same boundary, while Ancestry successor activation is one atomic transition after every pre-cut proposal has committed and read back its exact nonauthoritative terminal.

A collision-fenced external Effect is never retransmitted: only a bounded same-Operation-ID, same-generation read-only reconciliation chain may observe its started scope, and exhaustion leaves a permanent indeterminate terminal with the possible scope fenced. Ancestry proposal eligibility likewise uses the atomic first-page Commit Boundary, so queued work cannot become pre-cut through completion timing or handler order.

Repository Root Recovery Identity Hash Schema Historical Reader Custody Copy Conflict Post-Clearance Confirmed Collision blocks new authority immediately. An external Effect already begun may finish only Repository Root Recovery Identity Hash Schema Historical Reader Custody Copy Conflict Post-Clearance Confirmed Collision In-Flight Effect Reconciliation for its exact started scope, producing deterministic nonauthoritative terminal evidence and never a new root-authority publication or follow-on Effect.

Repository Root Recovery Bounded Ancestry Schema Successor Terminal Ordering gives activation and zero-publication cancellation one predecessor CAS and makes the first page forward-only. Provider Immutable Object Generation No-TTL Availability Drill Staleness Terminal Ordering serializes cadence misses against later success, and Trusted-Time Profile Revocation terminalizes the old attempt before a fresh successor-Profile chain begins; arrival order, result translation and cross-chain voting select nothing.

Repository Root Recovery Bounded Disagreement Attempt Chain Schema Successor Terminal Ordering linearizes activation against exact zero-publication cancellation on one predecessor/successor CAS; the first page permanently removes cancellation, and uncertain responses reconcile only the same identity. Reader and Fresh Custody Copy Conflict Confirmation Disagreement Attempt Chains likewise permit one active fixed-generation attempt, with Capacity Exhaustion preserving the provisional fence rather than selecting a result.

Repository Root Recovery Evidence Extension Manifest Canonical Partition Schema Recovery Unknown-Kind Set Post-Publication Omission Post-Ancestor Quarantine atomically captures the complete uncertain lineage rather than narrowing or appending membership. Every governed disagreement Schema Successor may cancel only at zero publication; its first page is forward-only, one-good repair is byte-exact, zero-good loss is terminal, and Fresh repair distinguishes hash collision from ordinary readback disagreement.

A post-publication omitted evidence kind becomes a Root Integrity Incident rather than an append, and a Late-Proof Successor Quarantine Manifest becomes forward-only after its first page with registered one-good repair and zero-good authority loss. Provider Immutable Object Generation Revalidation Horizon Dependent Authority Migration Expiry Terminal Ordering gives migration and expiry one trusted-time CAS winner without grace or provider-clock selection.

Fresh custody becomes forward-only at Repository Root Recovery Evidence Storage Successor Bootstrap Reserve Authority Loss Fresh Custody Genesis Publication Reconciliation, and zero-good required bytes produce a new-lineage terminal rather than rollback. A post-commit late Projection Proof advances through an immutable successor quarantine Manifest, while finite-Horizon authority cuts over only through an atomic Dependent Authority Migration Record.

Atomic recovery transitions now carry Repository Root Integrity Post-Ancestor Evidence Cut Source Set Successor Cancellation Carried Operation Set and interpret terminals only through Repository Root Integrity Post-Ancestor Evidence Cut Terminal Proof Registry. Provider Snapshot Response Evidence Allowlist Revocation Affected Authority Closure Disagreement Attempt Chain remains one-active and fail-closed; its capacity exhaustion cannot select or merge competing closures.

Source Set membership change may cancel only at zero publication; its first page fixes one identity and makes publication forward-only with permanent written-byte charges. Retained-route prune fixes all objects before the first delete, never rolls an object back and keeps the route Retained until every member is absent and its Receipt is readable. One valid unsafe Allowlist result atomically revokes that version and fences all possible dependents; later agreed-safe evidence can refine incident scope but cannot restore the version.

Repository Root Recovery Governance Schema Reader Support Set Successor Publication Reconciliation and Provider Snapshot Response Evidence Allowlist Successor Publication Reconciliation may cancel only before zero publication; their first page fixes one identity and makes reconciliation forward-only, with permanent written-byte charges, one-good-copy repair and zero-good lineage loss. Old versions remain authoritative until atomic activation. The first unsafe Allowlist result applies Provider Snapshot Response Evidence Allowlist Revocation Provisional Safety Fence, only network-started calls may finish, and two fresh Jobs must confirm classification and Provider Snapshot Response Evidence Allowlist Revocation Affected Authority Closure. Disagreement uses Provider Snapshot Response Evidence Allowlist Revocation Safety Disagreement Attempt Chain; capacity exhaustion preserves the fence instead of selecting a result.

Root artifact publication fixes one Repository Root Recovery Ceremony Intent and Descriptor content address; after the first page, exact Publication Reconciliation advances only the same identities and cannot roll back. Queue activation and Cleanup Abandon Terminal, Reader Support Set successor activation, fresh Repair Registry Recovery Lineage Genesis and every schema successor use exact current-tree CAS/readback rather than provider state. Evidence Cut commits only after its fixed Operation Index drains. Projection Proof admission atomically updates the Derived-Proof Dependency Index and reserves its future fence; one unsafe result applies Root Recovery Candidate Downstream Attribution Probe Projection Provisional Safety Fence immediately, two-job disagreement advances through one active attempt chain and capacity exhaustion leaves the fence permanent. Quarantined Repair CAS binds a Provider Immutable Object Generation that cannot change afterward.

Repository Root Recovery Artifact Publication Order commits pages, flat manifests, Descriptor and Receipt only in that order; every failure reconciles the same durable predecessors. Repository Root Integrity Post-Ancestor Evidence Cut drains pre-lock effects before atomically fixing source generations, and Repository Root Recovery Evidence Storage Successor must finish before any Manifest publication. Root Recovery Candidate Success Merge Queue Replacement Provisioned Nonauthoritative remains isolated until current-tree activation rather than provider readback. Repository Root Recovery Governance Schema Reader Support Set Capacity Reserve precedes its ceremony. Artifact Repair Type Registry Schema Successor Publication Reconciliation is exact-identity and forward-only; zero good staging pages produce Artifact Repair Type Registry Schema Successor Publication Capacity Terminal without cancellation or a new successor. Projection Revocation uses its pre-reserved record, and Quarantined Repair Mutation Revalidation Generation Fence repeats full readback immediately before CAS.

Repository Root Integrity Post-Ancestor Evidence Manifest Schema and Repository Root Integrity Post-Ancestor Evidence Manifest Capacity Reserve bind every page before restoration publication; Repository Root Integrity Post-Ancestor Evidence Manifest Derivation Block advances only through Repository Root Integrity Post-Ancestor Evidence Manifest Derivation Attempt Chain, whose Repository Root Integrity Post-Ancestor Evidence Manifest Derivation Attempt Chain Capacity Exhaustion requires Repository Root Integrity Post-Ancestor Evidence Manifest Derivation Attempt Chain Schema Successor. Root Recovery Candidate Success Merge Queue Replacement Attempt Incomplete remains the sole active attempt, while every failed published identity creates Root Recovery Candidate Success Merge Queue Replacement Failed Attempt Retention Charge. Repository Root Recovery Governance Schema Reader Support Set and Repository Root Recovery Governance Schema Reader Activation Barrier apply per successor and reject partial/third states. Artifact Repair Type Registry Schema Successor Headroom Reserve cannot fund entries; Artifact Repair Type Registry Capacity Alert stops admission, and Artifact Repair Type Registry Successor Cancellation races activation only through Artifact Repair Type Registry Successor Terminal Ordering.

Provider Snapshot Token Secret Boundary excludes bearer bytes from durable evidence, while Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Snapshot Lifecycle orders resource release after evidence and terminal. Root Recovery Broker Bootstrap Credential Boundary Graph Repair Opaque Platform Random Source Classification never credits hardware-source change. Root Recovery Candidate Downstream Attribution Probe Projection Registry and Root Recovery Candidate Downstream Attribution Probe Projection Qualification Evidence fix the transform before execution. Repository Root Recovery Artifact Envelope keeps Receipt and Descriptor independently at most 64 KiB; Repository Root Recovery Evidence Extension Manifest and Repository Root Recovery Evidence Extension Manifest Capacity Reserve bind all bounded pages before publication. Repository Root Integrity Quarantined Repair Mutation clears only through its exact Repository Root Integrity Quarantined Repair Mutation Revalidation Proof without deleting history or charges.

Root Recovery Candidate Governance Schema Reader Support Set and Root Recovery Candidate Governance Schema Reader Activation Barrier make the Index and Ineligibility Set one atomic checked-`u128` activation. Artifact Repair Type Registry Schema, Artifact Repair Type Registry Capacity Reserve, Artifact Repair Type Qualification Evidence and Artifact Repair Type Registry Successor make each new type one fully qualified append; Artifact Repair Type Registry Schema Successor may widen counts only while healthy, while Artifact Repair Type Registry Capacity Exhaustion leaves existing entries active and blocks new ones. Registry corruption is Repository Root Integrity Incident, never recursive repair. Retention Governance Installation Replacement Evidence Universe Manifest Generator Integrity Incident revokes the exact generator in every phase. Retention Governance Installation Replacement Physical Capacity Reservation Readback Snapshot and Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Snapshot fix immutable provider revisions before two-job agreement. Root Recovery Broker Bootstrap Credential Boundary Graph Repair Entropy Platform Attestation Schema and Root Recovery Candidate Downstream Attribution Probe Projection fix canonical evidence before execution.

Root Recovery Candidate Success Merge Queue Replacement Attempt Chain preserves the same pending transition and slot across serialized queue candidates; Root Recovery Candidate Success Merge Queue Replacement Attempt Chain Capacity Exhaustion selects no queue, and Root Recovery Candidate Success Merge Queue Replacement Attempt Chain Schema Successor imports the whole chain under checked-`u128` in a new root ceremony. Repository Last-Known-Good Tree Selection Disagreement Attempt Chain Schema Successor does the same under fresh Authority/custodians for restore selection. Repository Root Integrity Post-Ancestor Evidence Manifest must atomically publish and read back the complete quarantine set and charges before unlock; no restored-tree CAS deletes or credits later external bytes.

Root Recovery Broker Bootstrap Credential Boundary Authority Graph Schema Reader Dependency Drain Terminal Ordering makes Reader Drain cancellation and activation mutually exclusive current-tree CAS outcomes; first commit wins and every third state derives Repository Root Integrity Incident. Retention Governance Installation Replacement Evidence Universe Manifest Integrity Incident is an exact zero-publication abort before either Proof or Manifest publishes and `IMPORT_MANIFEST_INTEGRITY` plus Failed Candidate Retention Charge afterward. Retention Governance Installation Replacement Physical Capacity Reservation Status Disagreement advances through one checked-`u64`, one-active Retention Governance Installation Replacement Physical Capacity Reservation Status Disagreement Attempt Chain without majority or smaller-value selection; its Retention Governance Installation Replacement Physical Capacity Reservation Status Disagreement Attempt Chain Capacity Exhaustion follows the publication phase and never infers loss. Confirmed post-Genesis loss clears only through Retention Governance Installation Replacement Physical Capacity Reservation Loss Recovery atomically activating one complete Retention Storage Successor over fresh hard Reservations on both routes and rebinding every charge.

Repository Root Integrity Candidate Slot Restoration commits exactly the slot state in the selected last-good tree. Root Recovery Candidate Success Merge Queue Replacement preserves the pending transition and occupied slot, blocks merges during replacement and requires same-transition revalidation after activation. Repository Last-Known-Good Tree Selection Disagreement, its one-active Repository Last-Known-Good Tree Selection Disagreement Attempt Chain and Repository Last-Known-Good Tree Selection Disagreement Attempt Chain Capacity Exhaustion never synthesize a restore point. Root Recovery Candidate Downstream Attribution Attempt Chain and Root Recovery Candidate Downstream Attribution Attempt Chain Capacity Exhaustion preserve the exact request and Profile boundary without retroactive Candidate failure. Root Recovery Candidate Governance Schema Successor jointly activates the Candidate Dependency Index and Ineligibility Set; Installation Replacement Candidate Attempt Chain Schema Successor atomically imports the complete old attempt/charge chain under checked-`u128` schema. Neither permits partial activation or continuity.

Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Disagreement and its one-active Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Disagreement Attempt Chain require byte-identical fresh-job cursor results; Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Disagreement Attempt Chain Capacity Exhaustion terminalizes and charges the candidate. Root Recovery Broker Bootstrap Credential Boundary Graph Repair Entropy Platform Isolation Proof Disagreement and its one-active Root Recovery Broker Bootstrap Credential Boundary Graph Repair Entropy Platform Isolation Proof Disagreement Attempt Chain likewise select no platform result; Root Recovery Broker Bootstrap Credential Boundary Graph Repair Entropy Platform Isolation Proof Disagreement Attempt Chain Capacity Exhaustion terminalizes the Candidate. Artifact Repair Type Registry is append-only and fixes each type's one-good repair and zero-good terminal before an incident.

Charge Admission Fence Release Reconciliation uses one deterministic transition ID and exact-generation CAS; unchanged predecessor stays closed, exact successor opens and any third state is Charge Admission Fence Release Integrity Incident plus Repository Root Integrity Incident. Evidence Universe Manifest Schema fixes at-most-64-KiB pages and checked-`u64` totals, and Evidence Universe Manifest Capacity Reserve covers the complete dual-route Manifest before publication. Evidence Universe Derivation Disagreement uses its one-active Evidence Universe Derivation Disagreement Attempt Chain; Evidence Universe Derivation Disagreement Attempt Chain Capacity Exhaustion terminalizes and charges the candidate. Evidence Universe Reader Capability Block permits only zero-publication abort to a fresh Tool-bound candidate or, after publication, `IMPORT_READER_UNAVAILABLE` terminal and charge. Late Evidence Impact Classification Disagreement globally freezes new authority under its Late Evidence Impact Classification Disagreement Attempt Chain, and Late Evidence Impact Classification Disagreement Attempt Chain Capacity Exhaustion terminates the Installation. Installation Replacement Candidate Attempt Chain and Candidate Attempt Capacity Reserve serialize candidates without a fixed retry count; Candidate Attempt Chain Capacity Exhaustion admits no untracked successor. Physical Capacity Reservation Loss maps by phase to zero-publication abort, failed-candidate charge, or current-Installation Fence and retention incident.

A Retention Storage Successor freezes an exact all-pool snapshot and blocks concurrent schema/charge creation, expands only pools that cannot fit their next complete reserve, and may repair from one good custody copy. Governance Exhaustion replacement imports only verified immutable evidence and exact logical charges into a fresh Installation with no Runtime or authority continuity. A terminal partial Coverage-schema workflow permanently consumes its sequences and must re-prove full remaining headroom. Fence-lineage recovery carries every consumed or failed Candidate into the successor Ineligibility Set before a fresh three-slot Receipt.

Before the Storage Snapshot, admission stops and every previously admitted charge-capable workflow must prove terminal or exact pre-commit cancellation; no late writer is budgeted or rebased. One fresh Installation genesis binds every coexisting installation-terminal cause, a complete Import Gap Manifest and capacity proof covering all imported charges, the next worst-case Set and Graph successors, audit floors and a separate Governance Reserve. Missing evidence stays explicit and cannot support imported claims. Post-commit Coverage work remains pending without timeout until its exact reconciler records an explicit terminal. Fence and Supersession lineage termination share one carryover rule over every dependent consumed identity, including a formerly successful Candidate but excluding never-started slots.

Charge drain begins with a current-tree generation fence, and every writer re-presents its admission generation at final CAS. Replacement import scope comes from the final verified Inventory plus complete authority, registry, charge and unresolved-operation closure, never directory listing. Both custody routes reserve full imported and worst-case physical bytes independently of logical charges. Genesis CAS rejects a newly changed predecessor head; zero-publication failure may retry, while a published candidate can only resume or terminate under the same identity before a later terminal-linked candidate. Late evidence is charged read-only history and never edits the Import Gap. Two reconciliation jobs must agree on Coverage terminal state. A carryover that cannot fit its complete identities plus the next three slots requires a larger-schema fresh Installation rather than truncation.

Charge admission reopens only at the Charge Admission Fence Release Boundary: an exact zero-publication abort opens a fresh generation after drain, successful replacement opens on its new pools, and terminal failure remains closed. Installation replacement performs a fresh full Evidence Universe Derivation at one fixed terminal head; an earlier weekly Inventory is only a fetch seed. Its canonical segmented Evidence Universe Manifest binds counts, logical and physical bytes, charge sum, segment roots and aggregate root in dual custody. Late Evidence Impact Classification triggers ordinary incident or revocation only when the evidence adds a current authority dependency; otherwise it remains audit-only, and insufficient capacity preserves the Gap without borrowing Governance Reserve. Every written failed-candidate object and charge enters the next Universe and Incident Set, while only exact unused reserve releases after terminal. Each route proves physical capacity through a provider-native hard reservation or an exactly splittable exclusive slot; free-space reports and soft quotas are not capacity proof.

Coverage headroom remains a protected formula floor: zero append releases only the workflow claim, while the first append freezes the exact successor workflow and all written rows. One serialized Retention Storage Successor may expand Set and Graph pools atomically while preserving separate accounting; failure of its independent reserve is terminal Retention Governance Exhaustion rather than reserve recursion. Fence-witness integrity conflict terminates the exact Fence/resource lineage and requires a fresh locked-root Receipt, Tool Registry and Namespace Generation lineage.

Coverage Audit Headroom Reserve is nonborrowable and may be consumed only by its exact wider-schema workflow. Once retention publication crosses its first-publish commit boundary, later corruption never refunds logical capacity: one good copy permits Artifact Repair, while zero good copies permanently consume capacity and block the successor. A custody-storage successor uses a separately established healthy-period installation-wide Retention Governance Reserve. Conflicting valid observations from the record-bound Fence repair witness permanently block that Barrier and require exact witness qualification revocation; no replacement witness may finish it.

Coverage audit schemas reserve a formula-derived worst-case successor workflow plus one alert/response pair before any new governance Intent. Schema-retention capacity remains releasable only before the first external manifest, receipt or page reference; afterward the complete logical charge is permanent even on partial publication or cancellation. Exhaustion blocks only new Set-Schema successors and can expand only through a healthy prequalified custody-storage successor. Each Fence permanently binds the repair witness active at its commit; later Registry rotation affects only future Fences.

An Ancestry Schema Successor interrupted before activation resumes only its exact identity; canceled publication is permanent inert evidence and never creates dual-read. Coverage and Receipt Debt audit chains may continue through a wider schema only when it was already active under healthy authority before the next reservation fails. Receipt Debt sequence exhaustion blocks new governance and Policy but neither reverses a completed Barrier nor stops existing valid Runtime authority. Fence repair requires the preincident assigned read-only witness after byte repair; witness loss leaves the Barrier blocked without substitution.

An ancestry counter may continue only through a wider schema already active before its checked sequence is exhausted; otherwise governance terminates without wrap or a fresh chain. Graph and Fence custody repair require one exact good route; zero good copies are never reconstructed. A committed Fence remains active while conditional byte repair and its assigned observation restore the second route, and Resource Successor Barrier waits for that observation and complete readback. Derived debt remains one daily aggregate audit set, not per-member authority.

A tuple changing failure-domain triple uses one Registry CAS that reserves the new bounded domain, removes old current membership and rechecks both incident indexes; it remains in the old domain until commit. A committed Fence is immediately irreversible. Partial custody publication reconciles only the same bytes and blocks Resource Successor Barrier until both routes read back; rollback, changed membership, replacement Record and one-route completion are forbidden. After dual-route durability, missing deterministic member Receipts are daily alerted debt but cannot block the Barrier.

Each namespace failure-domain triple is admission-bounded to 256 active or pending tuples and reserves one complete at-most-64-KiB Fence Record before activation. A Namespace Generation Integrity Incident commits and indexes that complete set once, so every member becomes fenced without per-resource partial visibility. After the record is read back through both custody routes it alone satisfies Resource Successor Barrier fence evidence; derived per-resource exhaustion Receipts remain mandatory alerted audit backfill but cannot create authority or block the Barrier.

After Bootstrap sets the first expected Event Sequence beyond any durably declared Crash Tail Boundary, the Runtime Core accepts only exactly contiguous Core Events for that daemon run. For each one it stages a complete candidate state or controlled delta, ordered audit and client outcomes, and a stable Effect list with explicit dependencies. It verifies all production Core Invariants before atomically committing and exposing any Effect; failure preserves the prior state and emits nothing, without requiring a full state clone. Valid events forbidden by current domain state return structured audited Domain Rejections. Unexplained in-run Event Sequence duplicates, regressions, or gaps and internal invariant failures instead enter global Core Fault: new work stops, the outer daemon best-effort records diagnostics, requests Device Executor Shutdown, fails live connections, and exits so nonpersistent runtime state is cleared. Effect Results for known-completed operations are audited duplicates, while unknown, late, or generation-mismatched results are rejected; none can reapply state. The P0 Core Gate requires deterministic fixed-seed replay across example, property, generated state-machine, and fault-injection tests covering sequence reservations and crash-tail boundaries, scheduling, deadlines, resource modes, Admission, Reservations, Residency, cancellation and output races, Device Executor Failure, and audit degradation, without making any MLX performance or correctness claim.

Atomic validation does not authorize a complete-state scan on every Transition. Each private Core Module maintains bounded incremental invariant witnesses for its own state; the Transition Coordinator checks only the declared cross-Module conservation rules. Bootstrap, replay, tests, and explicit diagnostics retain full-state validation. Production tests assert examined entities, copied bytes, allocations, Candidate counts, and invariant work against the Hot-Path Work Budget rather than relying only on elapsed-time benchmarks.
