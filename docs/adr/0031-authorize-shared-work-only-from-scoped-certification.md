# Authorize Shared Work Only from Scoped Certification

Compatibility Custody Integrity Lifecycle Scenario Alias Manifest Failure Recovery Boundary requires a separately identified new Manifest generation, complete Evidence Custody and Closure qualification, and two failure-domain-independent Jobs agreeing on the complete mapping; the failed generation remains evidence and only future dependent qualification or terminology migration resumes. Compatibility Custody Bounded Integrity Lifecycle Specialization Catalog Published Inactive Boundary Successor Publication Cut serializes exactly one successor publication against the exact current Catalog head and permanently prevents activation of every older inactive Catalog afterward. Competing or stale candidates remain nonauthoritative and must be rederived and requalified against the new head, so fresh Policy selects only the unique current qualified head and never chooses a fork, first-observed winner or automatic activation.

Scenario Alias Manifest disagreement, omission or damage blocks only mapping-dependent new qualification and terminology migration; historical authority continues under original names and Schemas, so neither whole-tree nonreadiness nor nearest-map selection follows. A published Catalog whose Policy activation fails remains permanent authoritative but inactive evidence. While no successor exists, a later independent activation may select the same exact Catalog after fresh Readiness and authorization; changed bytes or dependencies require a successor, and successor publication makes the predecessor permanently ineligible without automatically activating either generation.

Schema compatibility requires an independently qualified per-axis record binding exact old/new identities, complete golden fixtures and the full required reader matrix; semver, additive shape and parse success grant no authority. Scenario Alias Manifests may guide only governance and fresh qualification and never reinterpret historical or runtime authority. A failed unpublished specialization step restarts under a new candidate or attempt, while each published Schema, qualification or Catalog boundary remains permanent and later work proceeds only forward from that exact boundary.

A new specialization gains authority only through four separate ordered boundaries: Schema publication, independent qualification, Catalog successor publication and later Policy activation; no publisher, change or runtime combines adjacent steps. An unknown or unsupported applicable specialization preserves its exact bytes but produces Migration No Path and nonreadiness for the complete affected scope, with no nearest-kind, generic-custody or Policy fallback.

Shared authority accepts only a Lifecycle specialization and Schema already present in the qualified Specialization Catalog and only after its complete Applicability Proof verifies. A Scenario Alias preserves exact historical meaning but grants no certification through textual similarity, while an unqualified kind, mutable Policy mapping or apparently absent stored Incident cannot establish authority.

Two Jobs count as independent only through an Independent Job Failure-Domain Proof covering every implementation, reader, provider/control-plane, credential and execution dimension relevant to the claim; different IDs, processes, hosts or machines are insufficient when a relevant domain is shared. Audit agreement never substitutes for that proof or scoped certification, and Current-Tree authority requires the complete Readiness Closure rather than an apparently unaffected subset.

Restored retired-generation queries and late reader Evidence Generation Proofs remain audit-only after their Indeterminate terminals; they may seed only separately identified, fully qualified generations and never backfill old authority. If a later-certified query drifts semantically, a new Semantic Integrity Incident fences its complete operation, reconciliation and Tool/Capability closure until a new identity is certified. Independent audit lineages must qualify new reader semantic identities and fixtures, while a shared-code Closure disagreement fences the complete possible reader universe.

Conflicting valid provider-native Execution Generation Proofs fence the complete operation, reconciliation and Tool/Capability-dependent authority closure; sequence, Receipt order, schema or provider preference cannot select one. If the proven old generation is not exactly queryable, or an in-flight reader response lacks immutable Evidence Generation binding, the operation is permanently Indeterminate and no current generation, cached response, adjacent endpoint or mutation probe substitutes. A later certification applies only to future or separately identified fresh-root reconciliation generations. A shared reader-code defect immediately fences every semantic identity using that lineage and its complete Reader, Descriptor, Bundle and Bootstrap Type Set closure until a new code lineage is fully recertified.

A rollback-spanning provider response is usable only with immutable provider-native Execution Generation Proof binding Operation ID, first execution and response to the certified implementation generation; schema inference leaves an Indeterminate terminal. Reader work already begun under conflicting revocation order is limited to old-identity read-only reconciliation, and byte-identical replacement reader code records one shared-code failure domain rather than counting as independent implementation evidence.

Rollback authority begins at the first provider call, not credential delivery: delivered-but-unstarted work cancels and revokes its old credential, while already begun read-only work reaches only its old deadline and reconciliation. Conflicting revocation-generation proofs fence the entire reader/successor universe, proof revocation governs the complete affected Effect scope, and a replacement Bundle uses wholly new independently qualified semantic component identities even when payload bytes repeat.

Provider implementation rollback creates a new read-only Semantic Identity and requires full certification even when schemas and code bytes repeat. Revocation evidence and provider-generation creation require native sequence proof; proof revocation fences every dependent, a missing historical Escalation Matrix blocks clearance rather than selecting the latest policy, and newly qualified readers cannot revive an agreed-invalid Bundle identity.

A read-only reconciliation operation is authorized by a semantic identity binding provider contract, implementation generation, schemas, side-effect contract and credential scope; drift quarantines the operation and dependent reconciliation until a new identity is certified. Critical pressure may terminate only through certified safe-point/watchdog semantics, and Reservation plus Pending Reclaim remain charged until ownership release and fresh Resource Evidence.

Only Tool Registry operations whose Certification Record proves exact endpoint, method, parameter, credential and nonmutation semantics may reconcile an uncertain external Effect; an unsupported query is itself an Indeterminate Terminal. Formula changes and cancellations retain old reservations and Pending Reclaim through safe terminal, readback, cleanup and fresh Resource Evidence, while declaration incidents escalate only through certified correlation identities.

Every recovery Resource Reservation captures its exact Capacity Formula Identity, Certification Record, aggregate and Tool/Capability generation. A later formula applies only to new Admission; invalid declaration identity or encoding rejects before state and quarantines the exact Capability, externally started old-identity work reaches only its pre-reserved safe terminal, and unexposed work cancels for explicit re-Admission.

Repository Root Recovery Bounded Recovery Capacity Formula Identity is certification-owned: its Tool Registry implementation and Certification Record bind formula schema, exact inputs, arithmetic widths and maxima. The Backend exposes that identity with one checked conservative aggregate; Admission verifies only the identity and capacity, Aggregate Overflow rejects before reservation, and any observed underestimate is a Bound Violation requiring exact Tool/Capability quarantine and recertification rather than a dynamic Admission formula.

Repository Root Recovery Identity Hash Schema Historical Reader Custody Copy Conflict Post-Clearance Confirmed Collision immediately fences every dependent authority created after Clearance while preserving its history, and fixed Reader or Fresh Custody route-generation loss authorizes neither no-conflict nor a newer-generation substitute. Schema Successor Authority Loss Affected Authority Closure Disagreement keeps the complete possible decision-dependent universe fenced, while Staleness Aggregate loss and late Expiry evidence cannot authorize shared work.

Shared work remains blocked by Repository Root Recovery Identity Hash Schema Historical Reader Custody Copy Conflict Provisional Authority Fence until exact two-Job Provisional Authority Fence Clearance, which affects only future decisions and never erases the observation. Repository Root Recovery Bounded Disagreement Attempt Chain Schema Successor Authority Loss Affected Authority Closure and the lost-Manifest Affected Authority Boundary Disagreement conservatively fence every possible dependent; only exact proved-disjoint outside authority may resume.

Terminal Proof remediation now treats unequal qualification as Repository Root Integrity Post-Ancestor Evidence Cut Terminal Proof Registry Proof Type Remediation Claim Disagreement and preserves different old/new classifications as Remediation Reclassification Discrepancy under the complete Effect Scope Fence. Successor Manifest Authority Loss uses its full affected boundary, and Migration/Expiry Trusted-Time Disagreement authorizes no winner until qualified time evidence agrees.

Terminal Proof recovery now requires Repository Root Integrity Post-Ancestor Evidence Cut Terminal Proof Registry Proof Type Remediation Claim and exact reclassification of the predecessor affected closure. Closure disagreement remains fenced behind its bounded two-Job chain, while Provider Snapshot Response Revalidation Verifier Tool Nondeterminism Recovery requires a separately qualified Tool/Profile successor and fresh revalidation of the original generation.

Scoped recovery certification now permanently revokes unsafe Terminal Proof type versions through Repository Root Integrity Post-Ancestor Evidence Cut Terminal Proof Registry Proof Type Revocation Affected Authority Closure. Repository Root Integrity Post-Ancestor Evidence Cut Effect Scope Disjointness Proof Disagreement Attempt Chain and Provider Immutable Object Generation No-TTL Availability Drill require fresh two-Job agreement; majority, inferred scope and stale availability cannot authorize shared work.

Scoped recovery certification now requires Provider Snapshot Response Revalidation Verifier Independence Profile, Repository Root Integrity Post-Ancestor Evidence Cut Effect Scope Disjointness Proof and Provider Snapshot Response Evidence Allowlist Remediation Claim. Shared or recovered authority cannot derive from correlated verifier builds, assumed scope separation or a relabeled response policy.

Repository Root Integrity Post-Ancestor Evidence Cut Terminal Proof Registry requires a source-specific provider-native signed proof type and two fresh agreeing Jobs; API status, log absence and custodian assertion are not terminal evidence. Provider Snapshot Response Revalidation Disagreement immediately applies Provider Snapshot Response Revalidation Provisional Authority Fence. Its bounded Attempt Chain may classify provider mutation/loss or verifier-tool nondeterminism, but the fence remains until the corresponding provider replacement or Tool Registry successor requalifies the evidence. Provider Immutable Object Generation Revalidation Horizon Identity Boundary prevents later policy or provider TTL from shortening an artifact obligation.

Post-Ancestor Evidence Cut fixes Repository Root Integrity Post-Ancestor Evidence Cut Source Set Successor membership and a fully reserved Repository Root Integrity Post-Ancestor Evidence Cut Operation Index Schema before drain. Repository Root Integrity Post-Ancestor Evidence Cut Reconciliation Capability Loss cannot be converted to absence by timeout: only provider-native terminal evidence plus Repository Root Integrity Post-Ancestor Evidence Cut Effect Scope Fence can make a fresh trust-root ceremony eligible, otherwise the root lock is permanent. Provider Snapshot authority likewise requires Provider Snapshot Response Equality Mismatch Evidence, Provider Snapshot Response Evidence Allowlist Dependency Index and exact-generation availability through Provider Immutable Object Generation Revalidation Horizon. Provider Immutable Object Generation Premature Loss or Mutation Incident fences the full dependent Snapshot, Profile, ceremony, Policy and Runtime closure.

Shared work rejects open Artifact or Storage Publication Reconciliation, Evidence Cut Drain Pending, Provisioned Nonauthoritative queue without Terminal Ordering, unsupported Reader Set successor/schema, mutable Provider Immutable Object Generation, revoked Allowlist version, unindexed Attribution Proof and every Root Recovery Candidate Downstream Attribution Probe Projection Provisional Safety Fence, Safety Disagreement or incomplete Revocation Affected Authority Closure. Only exact Permit-consumed operations whose network execution began may finish during a Projection fence. A two-job disjointness result over the complete Closure may resume an unaffected Runtime; logs, current process state, cached Projection Qualification or historical Candidate success never substitute.

Shared authority rejects a Root Recovery Candidate Success Merge Queue Replacement Provisioned Nonauthoritative queue, an incomplete Repository Root Recovery Artifact Publication Order, a missing Repository Root Recovery Governance Schema Reader Support Set Capacity Reserve, and any Artifact Repair Type Registry Schema Successor Publication Reconciliation or Publication Capacity Terminal. Root Recovery Candidate Downstream Attribution Probe Projection Revocation immediately removes every derived proof from authorization, blocks the exact dependent Profile and returns affected failures to Attribution Incomplete without changing historical Candidate success. Projection Registry Capacity Exhaustion leaves only existing unrevoked entries eligible; Provider Snapshot Response Evidence Allowlist and Quarantined Repair Mutation Revalidation Generation Fence must also verify before their dependent authority can participate in shared work.

Shared work remains blocked by any Repository Root Integrity Post-Ancestor Evidence Manifest Derivation Block or active Derivation Attempt Chain, Root Recovery Candidate Success Merge Queue Replacement Attempt Incomplete, missing Repository Root Recovery Governance Schema Reader Activation Barrier, unqualified Projection Registry entry, secret-bearing provider snapshot, oversized or unreserved Repository Root Recovery Artifact Envelope, or Repository Root Integrity Quarantined Repair Mutation without Revalidation Proof. Artifact Repair Type Registry Capacity Alert blocks only new repair-type admission; existing qualified types remain usable. Opaque Platform Random Source Classification may support isolation only through the stricter verified Tool, OS and independent-host changes and never asserts hardware-source change.

Shared work requires the exact Retention Governance Installation Replacement Physical Capacity Reservation Readback Snapshot and Root Recovery Broker Bootstrap Committed Credential Containment Audit Chain Genesis Cursor Snapshot rather than mutable provider state; the exact Root Recovery Broker Bootstrap Credential Boundary Graph Repair Entropy Platform Attestation Schema rather than narrative independence; and Root Recovery Candidate Downstream Attribution Probe Projection rather than production replay. Retention Governance Installation Replacement Evidence Universe Manifest Generator Integrity Incident revokes the exact generator until a remediated Tool Registry successor qualifies. Root Recovery Candidate Governance Schema Reader Activation Barrier, Root Recovery Candidate Success Merge Queue Replacement Attempt Chain and Repository Root Integrity Post-Ancestor Evidence Manifest must be terminal and current before related authority resumes. Artifact Repair requires an integrity-valid Artifact Repair Type Registry Schema entry and Artifact Repair Type Qualification Evidence; Registry Capacity Exhaustion does not revoke already qualified types.

Root Recovery Broker Bootstrap Credential Boundary Authority Graph Schema Reader Dependency Drain Terminal Ordering linearizes cancellation and activation by current-tree CAS: the first commit wins, uncertainty reads back the same transition and any third state is Repository Root Integrity Incident. Retention Governance Installation Replacement Evidence Universe Manifest Integrity Incident, Physical Capacity Reservation Status Disagreement and its Attempt Chain, Audit Chain Genesis Cursor Disagreement and its Attempt Chain, Entropy Platform Isolation Proof Disagreement and its Attempt Chain, Repository Last-Known-Good Tree Selection Disagreement and its Attempt Chain, and Candidate Downstream Attribution Attempt Chain never authorize shared work while unresolved. Their Capacity Exhaustion terminals select no result. Artifact Repair is authorized only by the active Artifact Repair Type Registry. Confirmed post-Genesis Reservation Loss restores charge authority only through Retention Governance Installation Replacement Physical Capacity Reservation Loss Recovery with one complete Retention Storage Successor, fresh hard Reservations on both routes and complete charge rebinding/readback.

Repository Root Integrity Candidate Slot Restoration, Root Recovery Candidate Success Merge Queue Replacement, Root Recovery Candidate Governance Schema Successor and Installation Replacement Candidate Attempt Chain Schema Successor grant authority only after their exact atomic restoration, replacement, joint-schema activation or complete legacy import boundary. A pending queue replacement retains its slot but grants no merge authority; a Profile blocked by Root Recovery Candidate Downstream Attribution Attempt Chain Capacity Exhaustion cannot be certified under the same identity; and Entropy, Cursor, Reservation or Last-Good attempt-chain capacity cannot be converted into a majority or operator-selected result.

Nonce History Capacity Exhaustion continues only through Graph Repair Nonce History Schema Successor active at fresh genesis: canonical checked-`u128` segments reference every old `u64` entry without rewriting it. Entropy Platform Incident additionally requires Graph Repair Entropy Platform Isolation Proof changing the entropy implementation and at least one OS, hardware-random-source or host failure domain. Every Source Loss disagreement attempt closes with one typed Source Loss Proof Disagreement Attempt Terminal in `SOURCE_PRESENT`, `SOURCE_LOST`, `DISAGREED` or `INCOMPLETE`. Reader removal uses a paged Reader Dependency Drain Intent Operation Index and complete Reader Dependency Drain Intent Capacity Reserve before its fence; insufficient capacity starts nothing, while explicit Reader Dependency Drain Cancellation before successor publication retains the reader and opens a fresh admission generation.

Every Graph repair uses a fresh single-use Intent whose cancellation or first write permanently consumes it. A post-integrity fresh Graph lineage preserves the last active schema and cannot combine disaster recovery with schema evolution; loss of the final verified source retires the old Installation and all Runtime authority. Reader membership changes through a same-schema checked Support Set successor atomically selected with Tool Registry, never a mutable list.

Each repair nonce is checked against complete permanent history before signature; a third collision closes that repair ceremony as a no-write Randomness Incident. Graph Source Loss requires two independent jobs to inspect both configured routes plus two last-certified custodians; unavailable or partially inspected custody remains unknown and frozen. Verified Graph bytes with no runnable qualified old-schema reader instead produce a distinct Reader Loss terminal with the same fresh-Installation/no-continuity outcome, never an emergency build or disaster schema conversion. Healthy reader removal first stops new dependent operations and proves every fixed existing dependency terminal through two independent jobs before the atomic Support Set and Tool Registry successor.

Nonce history is a checked permanent chain with full three-proposal-plus-Incident capacity reserved before the first proposal; collision or capacity history never resets across trust roots. A Randomness Incident recovers only through a full out-of-band Receipt selecting a fresh Tool/entropy implementation and fresh jobs while preserving the exact Graph and schema. Valid Source Loss disagreement stays unknown without majority, and a source found after the terminal is charged read-only evidence that cannot revive authority. Reader drain has no timeout: an unreconciled operation leaves the existing reader active and its successor pending.

Randomness recovery may retain the exact locked Root Authority only after independent Graph Repair Randomness Recovery Authority Retention Proof; Tool, entropy implementation and recovery Jobs are replaced, while any Authority concern uses Disaster Bootstrap. A second Graph Repair Randomness Incident after one completed recovery in the same Installation is an Entropy Platform Incident and terminates that Installation. Nonce History Capacity Exhaustion likewise requires a fresh Installation with a larger versioned history schema active at genesis and complete history import, with no continuity. Source Loss Proof Disagreement uses one checked-`u64`, one-active Attempt Chain reserving its start and mutually exclusive terminal before execution; its capacity exhaustion or permanent loss of a required historical custodian terminates the Installation without threshold reduction or substitute witness. Reader drain state is only the persistent current-tree Reader Dependency Drain Intent binding fence generation and exact operation-set root; restart or job replacement reconstructs from that Intent and no Runtime scan.

Graph repair revalidates its exact head, route and participants immediately before the first write; pre-write change cancels the unused proof, while post-write compromise terminates the Graph lineage and forces full security/disaster recovery. Graph schema activation atomically verifies immutable support Receipts and makes predecessor-only readers ineligible without dual-read or rollback. The MVP never upgrades incident reconciliation to mutation, and direct compromise, Closure membership or revocation of its observation node disables reconciliation without failover.

Graph repair begins only after two recovery jobs reproduce the provisional Closure from the sole good custody route and two last-certified custodians sign the same eligibility bytes. Completion remains tied to those exact jobs and custodians; conflicting valid results create a Graph Repair Integrity Incident and global Freeze. Graph schema activation requires support from every Recovery Tool Bundle, verifier job and custodian validation path plus a permanent retention contract. Operation Dependency Set includes every observation, evidence and read-only node actually used; incident reconciliation remains non-propagating, and a discovered need to mutate creates a Mutation Finding without upgrading the capability.

Graph Repair Capability is an explicit `CAN_MUTATE` graph node and is unusable when its provisional or certified Closure is affected. Completion uses the non-executing recovery job plus two custodians and then ordinary Closure Certification, without adding a third witness. A Graph Schema successor proves per-seed Closure monotonicity, fails closed for unknown readers and resumes only its exact pending identity. Runtime dependency sets are system-derived from canonical roots, never caller authority. Calls already begun may perform only exact-ID read-only reconciliation through a prequalified incident capability; no broad credential or Runtime resume is implied.

Graph repair during Security Freeze uses only a Tool-Registry-pinned recovery capability qualified while healthy, two recovery jobs and two last-certified custodians; ordinary Broker authority and incident-time capability creation are forbidden. Propagation semantics evolve only through a healthy one-way full-graph Schema Successor. Closure cannot be certified with fewer than two last-certified custodians, and every resumed Runtime boundary supplies a complete at-most-256-node, at-most-64-KiB disjointness proof. A provider-independence incident blocks every new boundary while calls whose network execution already began may finish only under their original deadlines and reconcile.

Affected Closure follows only capability/control/signing/secret edges; receipt and observation edges are evidence-only, and shared mutable control is bidirectional. Two independent recovery jobs plus two last-certified custodians certify identical Closure bytes. Every resumed Open, dispatch and egress re-proves complete dependency disjointness, and any Graph/Closure change restores global Freeze. A later provider merger derives a separate Provider Independence Incident and requires all three Observer dimensions to change without adding a clock-pair failure.

Credential incidents first freeze all new authority. The certified Closure comes from a dual-route graph bounded to 256 nodes and 1,024 typed edges; only existing Runtime authority with a complete dependency set proven disjoint may then resume, while closure members and unknown dependencies remain frozen. Observer provider replacement is defined by provider-root/control-plane identity: both new providers are pairwise distinct and set-disjoint from the old pair, not merely different accounts or regions. Shared CA/DNS remains an explicit dependency and contributes no outage interval.

Repeated Observer pair failure is treated as platform correlation: after Observer Platform Trust Incident both Observers must move to replaced independently administered hosts, mutable control planes and providers, with ancestry preserved until full 86,400-second validation plus Drill resets it. Credential Scope Escape and post-final Containment Consistency are distinct incidents but share one Credential Boundary Security Recovery. That lane computes the exact transitive affected-authority closure, replaces and requalifies every member, and carries only immutable evidence and ineligibility ancestry; ordinary root ceremony, target-only repair, same-boundary resampling and partial replacement are intentionally insufficient.

Shared scheduling accepts only work whose exact Capability Key is covered by an immutable hash-bound Certification Record. The key binds Model Revision, phase, batch and shape bucket, Worker and MLX build identities, negotiated protocol capabilities, and a Certification Envelope containing the relevant Apple Silicon device class, GPU configuration, unified-memory size, and macOS version. Certification records retain compact environment, tool, case, correctness, bound, limitation, and external-evidence hashes; weights, large traces, builds, and bulky raw runs remain external or ignored. Current M3 Ultra 256 GB evidence cannot certify 32, 64, or 128 GB systems, and conservative Governor defaults or proportional scaling do not create an Envelope.

Runtime Clone retains every immutable Certification Record unchanged. During offline destination creation it derives applicability by comparing the complete Capability Key and Certification Envelope with the target's actual path, hashes, device, GPU configuration, memory, macOS, Worker and MLX builds, descriptor-bound Backend Protocol, and minor-scoped capabilities. This applicability result enters the Clone Receipt but not Semantic State Hash; an Envelope or exact-key mismatch merely leaves the affected work uncertified and never rewrites or broadens the record. Separately, artifact-identity or Backend compatibility validation failure, including a Model Descriptor mismatch, makes only that Revision Unavailable and therefore changes target state meaning. Neither result blocks unrelated Revisions or global Service Readiness.

The Rust daemon's Admission boundary exclusively derives Certification Applicability from fresh evidence and owns one fixed-capacity in-memory recency cache implemented by bounded LRU or clock replacement; the Worker and other modules may supply evidence but never cache or return an authorization decision. The key contains complete Generation Hash, exact Certification Record and Capability Key identity, Worker and MLX build identities, negotiated protocol descriptor and effective capability set, and environment fingerprint including device, GPU, memory, and OS facts. A change to any member invalidates the entry before further Admission; Worker restart, Control Mutation, protocol renegotiation, or fresh environment evidence cannot inherit an old answer. A miss synchronously recomputes applicability at Admission, while hit, eviction, and replacement change neither the decision function nor Audit evidence. The cache never enters Control State, Semantic State Hash, Runtime Bundle, Metadata Store, or Audit as authorization, and restart begins empty. Online Cost Profiles may update ordinary estimates only within fixed certified bounds. Exceeding an ordinary estimate calibrates it; exceeding a certified bound quarantines the most specific Capability Key, with explicit escalation for correlated systemic failures. Uncertified shapes are rejected in shared mode and may run only through explicit Control Plane Exclusive Mode when independent resource and Exclusive Safety Point bounds exist.
